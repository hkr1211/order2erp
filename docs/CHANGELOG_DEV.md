# 开发日志

## 2026-05-26

### 已完成

- 建立 ERP 查询中台基础服务，支持本地浏览器访问。
- 智邦 ERP API 已能按接口拉取数据，并逐步同步到 SQLite。
- 建立 SQLite 同步保护：冷却时间、小批量请求、ERP 健康状态、同步暂停开关。
- 把中文字段名、日期/数字解析等通用显示工具拆到 `src/displayUtils.js`。
- 完成 PMC 作战指挥台：红黄牌风险、订单作战地图、干预清单、闭环质量。
- 完成订单中心、报表中心、财务中心、物料采购中心、生产中心、车间看板等主页面；异常待办并入 PMC 红黄牌闭环，报价中心从主业务架构中停用。
- 完成用户信息维护，用于修正跟单员、财务、管理人员角色识别。
- 完成 SQLite 覆盖率、历史同步、ERP 请求日志等系统页。
- 完成外贸订单看板，按外贸出口或非 RMB 币种筛选订单。
- 完成车间电子看板：
  - 总览页 `/workshop-board`
  - 轧制大屏 `/workshop-board/rolling`
  - 冲压大屏 `/workshop-board/stamping`
  - 钨钼大屏 `/workshop-board/tungsten-molybdenum`
  - 轮播入口 `/workshop-board/rolling?rotate=1`
- 车间大屏按 `planned_start_date <= 今天 <= planned_finish_date` 统计进行中计划。
- 车间大屏字段已精简，隐藏管理字段“订单匹配方式”和“绑定销售订单”。
- 冲压工序已独立归入冲压板块，不再误入钨钼。
- 添加 `scripts/smokeWorkshop.js` 和 `npm run smoke:workshop`。
- 把车间看板页面渲染从 `src/server.js` 拆到 `src/pages/workshopBoardPage.js`。
- 把通用页面模板拆到 `src/pages/html.js`。
- 把图形化首页拆到 `src/pages/homePage.js`。
- 把通用 API 查询结果 HTML 展示拆到 `src/pages/apiResultPage.js`。
- 把 PMC 作战指挥台、早会文本、干预处理页拆到 `src/pages/pmcPage.js`。
- 把财务中心拆到 `src/pages/financePage.js`。
- 把订单中心和订单详情页拆到 `src/pages/ordersPage.js`。
- 把派工-订单人工绑定页拆到 `src/pages/procedureLinksPage.js`。
- 把角色工作台、跟单员工作台和跟单摘要页拆到 `src/pages/followupPage.js`。
- 把报表中心、日报打印版、CSV/Excel 导出和干预记录台账拆到 `src/pages/reportsPage.js`。
- 把系统状态中心拆到 `src/pages/systemPage.js`。
- 把 ERP 请求日志、SQLite 覆盖率、历史同步、安全窗口、同步暂停和 PMC 路线页拆到 `src/pages/systemToolsPage.js`。
- 把 ERP 请求日志、SQLite 覆盖率、历史同步中心等系统工具查询编排拆到 `src/queries/systemQueries.js`。
- 把用户信息维护拆到 `src/pages/userRolesPage.js`。
- 把用户信息维护、跟单候选识别和用户角色跳转结果编排拆到 `src/queries/userRolesQuery.js`。
- 把应收应付中心查询、本地财务兜底和收付款风险状态计算拆到 `src/queries/financeQuery.js`。
- 把物料中心、异常中心、物料任务和异常闭环状态查询拆到 `src/queries/materialExceptionQuery.js`。
- 把订单中心、订单穿透详情、本地订单分页和交期/缺料聚合拆到 `src/queries/ordersQuery.js`。
- 把 PMC 控制台、跟单工作台、本地 PMC 汇总和实时 PMC 聚合拆到 `src/queries/pmcQuery.js`。
- 把生产中心、派工追踪、车间看板和工作中心负荷查询拆到 `src/queries/productionQuery.js`。
- 把采购跟催中心查询、入库/应付组合跟催和供应商汇总拆到 `src/queries/procurementQuery.js`。
- 把报价归档、报价负责人汇总和外贸订单看板查询拆到 `src/queries/quotesQuery.js`；报价中心后续已从主入口停用。
- 把报表中心、日报导出数据源、排产甘特视图和插单影响查询拆到 `src/queries/reportSchedulingQuery.js`。
- 把干预记录台账、派工-订单人工绑定查询拆到 `src/queries/actionQueries.js`。
- 工序汇报历史同步支持智邦接口 `InDate_0/InDate_1` 添加日期过滤，并在安全窗口中检测重复页后自动停止告警；如果返回页全部已存在于 SQLite，也会自动停止并显示“新增行数/无新增停止”。
- 新增每日增量同步调度器：服务运行时每天北京时间 0 点同步前一天的销售订单、工序汇报、应收应付、采购和库存数据，并小批量刷新物料/库存告警；可通过 `DAILY_SYNC_ENABLED=0` 关闭。
- PMC 作战指挥台新增“轧制前道影响冲压/钨钼后道”的跨工段流转风险：同一销售订单下后道 3 天内要料且轧制未齐套时，自动进入前后工段断点、红黄牌和待干预动作。
- 跨工段流转风险复用派工-订单人工绑定/辅助匹配结果；即使 ERP 派工没有订单号，只要已经绑定到同一销售订单，也能识别轧制前道影响后道。
- PMC 新增前后工段监控覆盖率和缺口清单，提示“后道缺少销售订单号”或“已关联订单但没有可识别轧制前道”，便于 PMC 补人工绑定。
- PMC 新增“前后转序交接”清单：当轧制已完成或接近完成、冲压/钨钼 3 天内要料时，直接提示确认半成品入库/转序，避免后道等料。
- PMC 新增“数据可信度”区块和“数据需关注”KPI，显示销售订单、物料/库存告警、派工计划、库存明细、应收应付的行数、最近同步时间和可信状态。
- PMC 前后转序交接清单补充“确认已入库/确认已转序/后道已接收”按钮，可直接进入本地干预台账留痕。
- PMC 风险占比改为统一风险池口径：按红黄牌风险事项 / 监控事项计算，覆盖销售订单、派工、物料告警和财务风险，不再只按销售订单计算。
- PMC 新增“指挥模式”切换：老板/管理者可只看数据可信度、早会重点、红黄牌、待干预和前后工段闭环，隐藏跟单员、复盘、作战地图和原始明细。
- PMC 新增“风险来源汇总”：按风险类型聚合红牌数、黄牌数、责任角色、下一步动作和代表问题，帮助管理层快速定位主要瓶颈。
- PMC 新增“责任部门待办”：按责任角色聚合红牌、黄牌、待办数、主要风险和代表问题。
- PMC 红黄牌和早会重点新增“风险评分/评分依据”，按红黄牌等级、风险类型、到期/逾期状态和剩余/缺口数量给出透明优先级。
- PMC 红黄牌和早会重点改为按风险评分排序，同分时再按红黄牌、风险类型和日期排序，保证最高风险自动前置。
- PMC 新增“指挥结论”：从最高风险、责任部门压力和数据可信度自动提炼今日管理判断，放在 KPI 后作为老板/管理层第一眼结论。
- PMC “指挥结论”增强为管理建议：补充早会重点、责任人、反馈时限和需拍板事项，让页面直接回答“今天讨论什么、谁负责、多久反馈”。
- PMC 新增“早会行动清单”：从指挥结论生成行动编号、早会追问、要求结果、反馈时限和升级规则，把管理判断落到会议执行项。
- 派工人工绑定页新增 ERP 字段检查和建议绑定线索：识别派工计划接口未返回订单号字段，使用工序汇报“单据主题”编号尝试反查销售订单标题，并给出预填绑定或人工补充路径。
- 修正订单-工序覆盖率中的“未关联派工”统计口径：页面仍只展示前 30 条明细，但摘要显示真实未关联总数。
- PMC 订单-工序覆盖率新增“工序汇报主题匹配”：当工序汇报主题中的 5/6 位编号能唯一命中销售订单标题时自动补订单号；若多个订单命中同一编号则保持未绑定，交给人工确认。
- 把物料、采购、报价、外贸、生产、派工、异常、排产等业务中心页面拆到 `src/pages/operationsPage.js`。
- 精简主导航为 8 个入口：首页、PMC、订单、生产、车间看板、物料采购、财务、系统；旧 `/quotes`、`/exceptions` 自动跳回 PMC，报价同步不再进入默认同步策略。
- 新增 `docs/CODEX_HANDOFF.md`，用于减少后续上下文恢复成本。

### 当前验证

- `npm run check`：应作为每次提交前的基础验证。
- `npm run smoke:workshop`：用于验证车间看板总览、三工段大屏和轮播目标。

### 待优化

- 继续把 `src/server.js` 中剩余的路由胶水、API 返回和干预记录处理按领域拆分。
- 为 PMC、财务、跟单员工作台补充 smoke 脚本。
- 继续提升派工与销售订单自动匹配率。
- 梳理 90 天同步覆盖率，明确哪些页面仍缺明细数据。
- 后续如接入飞书自然语言问答，应优先读取 SQLite 和受控查询接口，避免直接打 ERP。
